## 1. Remove Menubar

- [x] 1.1 Remove menubar creation and `root.config(menu=...)` code from `main()`
- [x] 1.2 Bind `Escape` key to `root.quit` for closing the window

## 2. Add Minimal Header

- [x] 2.1 Create a `Frame` at the top with a `Label` showing the filename
- [x] 2.2 Update `render_markdown` to set the header label text on file open
- [x] 2.3 Style header with matching background, no border, subtle font

## 3. Simplify CSS

- [x] 3.1 Replace current CSS with minimalist styles: softer colors, no heading borders, lighter code backgrounds, reduced padding

## 4. Update Startup Message

- [x] 4.1 Change placeholder text to mention `Ctrl+O` keyboard shortcut
